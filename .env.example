# Database
DB_NAME=agent_console
DB_USER=postgres
DB_PASSWORD=change-me-strong-password

# JWT (generate: openssl rand -hex 32)
JWT_SECRET=change-me-generate-with-openssl-rand-hex-32

# App URL (for CORS)
APP_URL=https://your-domain.com

# Caddy domain
APP_DOMAIN=your-domain.com

# Optional
LANGFUSE_SECRET_KEY=
LANGFUSE_PUBLIC_KEY=
